<h2 class="text-center">Iniciar sesión</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, required: false, autofocus: true, placeholder: "juanitopatatas@culiplonto.com" %>
    <%= f.input :password, required: false, placeholder: "elcumpledelanovia" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Iniciar sesión", class: "btn btn-primary btn-block" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
